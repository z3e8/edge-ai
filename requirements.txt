Flask==3.0.0
# tensorflow wheels are platform-specific. this keeps installs working on x86_64,
# and uses the aarch64 build on raspberry pi 64-bit.
tensorflow==2.15.0; platform_machine != "aarch64"
tensorflow-aarch64==2.15.0; platform_machine == "aarch64"
Pillow==10.1.0
numpy==1.26.2
requests==2.31.0

